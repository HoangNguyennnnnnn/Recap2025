# Environment Variables
PORT=3000
NODE_ENV=development

# MongoDB Atlas Connection
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database>?retryWrites=true&w=majority
# Example: MONGODB_URI=mongodb+srv://myuser:mypassword@cluster0.xxxxx.mongodb.net/love-universe-2025?retryWrites=true&w=majority
# Get this from MongoDB Atlas Dashboard -> Database -> Connect -> Connect your application
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database>?retryWrites=true&w=majority

# Cloudinary Configuration
# Get these credentials from your Cloudinary Dashboard: https://cloudinary.com/console
# Free tier includes 25 GB storage and 25 GB bandwidth/month
CLOUDINARY_CLOUD_NAME=your_cloud_name_here
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here

# CORS Configuration
# Production: Add your deployed frontend URL(s) - comma-separated for multiple origins
# Example: ALLOWED_ORIGINS=https://your-app.vercel.app,https://custom-domain.com
# Development: http://localhost:5173 is automatically allowed
ALLOWED_ORIGINS=https://your-app.vercel.app

# Authentication Configuration
# Passcode for accessing the application (format: DDMMYYYY, e.g., your special date)
# Example: 14022020 for February 14, 2020
AUTH_PASSCODE=14022020

# JWT Secret for token signing
# Generate a secure random string for JWT token signing
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# JWT Token Expiration
# Format: https://github.com/vercel/ms (e.g., "7d" = 7 days, "24h" = 24 hours, "1y" = 1 year)
JWT_EXPIRES_IN=7d
